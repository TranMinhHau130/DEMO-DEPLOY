import { createMuiTheme } from "@material-ui/core/styles";

const themes = createMuiTheme({
  palette: {
    primary: {
      light: "#ec545d",
      main: "#e5101d",
      dark: "#fa0716",
    },
    newColor: "yellow",
  },
});
export default themes;
